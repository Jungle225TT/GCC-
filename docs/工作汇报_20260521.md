# 工作汇报（2026-05-21）

## 一、工作概览

5 月 21 日主要围绕 GCC 智库抓取系统完成三类工作：

1. 梳理 ToS 合规风险，明确不可直接爬取的网站及替代访问路径。
2. 执行当日抓取，并补跑 AI 标题翻译与结构化研究简报。
3. 将合规报告沉淀到代码层，优化高风险来源跳过、替代路径提示和 dry-run 导出。

## 二、ToS 合规梳理

基于以下文件完成了合规整理：

- `/Users/jungle/Desktop/期刊信源_ToS合规检查报告.docx`
- `/Users/jungle/Desktop/ToS_合规判断_前30.csv`

整理结果：

- 明确禁止网络爬虫或自动化抓取的网站：9 个。
- 自动化软件或批量下载需事先审批的网站：1 个。
- 为每个高风险站点补充了可替代访问路径，包括 Crossref、OpenAlex、Unpaywall、RePEc、官方 newsletter/RSS、TDM API、JSTOR dataset、机构订阅、OA 版本和书面授权等。

产出文件：

- `docs/GCC_不可爬取网站及替代访问路径报告.md`

核心判断：

- 这些网站不应纳入常规全文爬虫任务。
- 技术可爬不等于合规可爬。
- 对高风险来源，应拆成“发现层”和“正文层”处理：发现层可用元数据源，正文层只走 OA、订阅授权、TDM API、数据集申请或书面许可。

## 三、当日抓取运行

### 1. 普通抓取

运行方式：

```bash
./.venv/bin/python gcc_thinktank_scraper_v2.py --no-dedup --output-dir output
```

运行结果：

| 项目 | 结果 |
|---|---:|
| 目标智库 | 29 个 |
| 地区分布 | gcc×16 / mena×4 / western×9 |
| 时间窗口 | 近 30 天 |
| JS 渲染 | Playwright |
| AI | 未启用 |
| 去重 DB | 禁用 |
| 相关文章 | 58 篇 |
| 强相关 / priority_read | 10 篇 |
| 中等相关 / normal | 48 篇 |
| RSS 获取 | 17 篇 |
| 总耗时 | 约 3.9 分钟 |

输出文件：

- `output/gcc_research_20260521_1536.md`
- `output/gcc_research_20260521_1536.json`

### 2. AI 抓取与简报

确认 `DEEPSEEK_API_KEY` 可用后，启用 AI 重新运行：

```bash
./.venv/bin/python gcc_thinktank_scraper_v2.py --no-dedup --ai --output-dir output
```

运行结果：

| 项目 | 结果 |
|---|---:|
| 相关文章 | 61 篇 |
| 强相关 / priority_read | 9 篇 |
| 中等相关 / normal | 52 篇 |
| RSS 获取 | 17 篇 |
| 标题翻译 | 61/61 成功 |
| AI 简报批次 | 7 批 |
| AI 简报强相关 | 5 篇 |
| 抓取+筛选耗时 | 约 229.8 秒 |
| AI 标题翻译耗时 | 约 16.5 秒 |
| AI 简报生成耗时 | 约 229.2 秒 |
| 总耗时 | 约 7.9 分钟 |

输出文件：

- `output/gcc_research_20260521_1542.md`
- `output/gcc_research_20260521_1542.json`
- `output/gcc_summary_20260521_1542.md`
- `output/gcc_summary_20260521_1542.pdf`

运行中合规跳过来源：

- Brookings Doha Center
- Atlantic Council — Middle East Programs
- International Institute for Strategic Studies (IISS)

以上来源未使用 `--include-high-risk` 覆盖，保持默认合规跳过。

## 四、代码优化

根据 `docs/GCC_不可爬取网站及替代访问路径报告.md`，完成以下代码和规则优化：

### 1. 合规规则升级

文件：`compliance_rules.yaml`

更新内容：

- 版本升级到 `1.2`。
- 增加报告来源：`docs/GCC_不可爬取网站及替代访问路径报告.md`。
- 增加默认字段：
  - `fulltext_scraping_allowed`
  - `metadata_allowed_path`
  - `fulltext_allowed_path`
  - `requires_permission`
- 新增高风险域名规则：
  - `cambridge.org`
  - `tandfonline.com`
  - `direct.mit.edu`
  - `aeaweb.org`
  - `onlinelibrary.wiley.com`
  - `jstor.org`
  - `piie.com`
  - `journals.uchicago.edu`
- 更新现有高风险来源：
  - `brookings.edu`
  - `atlanticcouncil.org`

### 2. 主脚本优化

文件：`gcc_thinktank_scraper_v2.py`

优化内容：

- 合规规则现在会自动继承 `defaults`，避免旧规则缺少新增字段。
- 高风险来源跳过日志中增加“替代路径”提示。
- AI 简报末尾的高风险来源表新增“合规替代路径”列。
- `high_risk_sources_records()` 默认只列当前 `THINK_TANKS` 中实际会被跳过的来源，避免把非当前抓取源的期刊网站混入日常简报。
- 支持 `active_only=False` 输出全量不可爬取/需审批合规台账。
- dry-run 导出的 `blocked_sources.csv` 新增字段：
  - `metadata_allowed_path`
  - `fulltext_allowed_path`
  - `requires_permission`
  - `fulltext_scraping_allowed`
  - `analysis_source`

### 3. 回归测试补充

文件：`scripts/test_keywords_funnel.py`

新增测试：

- 检查 `blocked_sources.csv` 数据源必须包含人工浏览入口、元数据替代路径和全文授权路径。
- 检查 ToS 报告中的禁止/需审批域名已落入 `compliance_rules.yaml`。
- 检查默认运行清单不会混入非当前 `THINK_TANKS` 来源。

验证结果：

```bash
./.venv/bin/python scripts/test_keywords_funnel.py
```

结果：

```text
22 通过 / 0 失败 / 22 总计
```

## 五、环境处理

首次运行脚本时，系统 `python3` 缺少 `requests` 等依赖，因此创建并使用本地虚拟环境：

```bash
python3 -m venv .venv
./.venv/bin/pip install -r requirements.txt
```

Playwright Chromium 浏览器缓存已存在，可支持需要 JS 渲染的站点。

由于本地没有 `data/` 目录和 SQLite 去重库，当日运行均按项目约定使用：

```bash
--no-dedup
```

## 六、运行中观察到的问题

部分站点仍存在访问不稳定或反爬问题：

- GRC：连接超时。
- Future Center：403。
- MEI / CSIS：部分页面出现 403。
- 多个站点 RSS 返回为空或解析失败，脚本已按配置回退 HTML 或跳过。

合规层面：

- Brookings、Atlantic Council、IISS 已按规则默认跳过。
- 不建议使用 `--include-high-risk` 进行常规日报抓取，除非有单独授权或人工复核需求。

## 七、下一步建议

1. 将 `docs/GCC_不可爬取网站及替代访问路径报告.md` 作为后续信源合规台账的基础版本。
2. 若后续补完剩余 19 个 ToS 网站，应继续扩展 `compliance_rules.yaml`。
3. 日常运行建议使用：

```bash
./.venv/bin/python gcc_thinktank_scraper_v2.py --no-dedup --ai --output-dir output
```

4. 若要避免每日重复推送，应补建 `data/` 目录并启用 SQLite 增量去重。
5. 对 GRC、Future Center、MEI、CSIS 等不稳定站点，可后续单独排查选择器、路径或合规替代发现源。
